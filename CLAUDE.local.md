# CLAUDE.local.md

## Продакшн

- **Домен**: `teacherstory.it-joy.ru`
- **Сервер**: Москва (тот же, что VDole — 195.2.75.212, SSH :1022)
- **Каталог**: `/var/www/teacherstory.it-joy.ru/`
- **Порты**: 33001 (API), 33002 (dev HMR, не используется на проде)

## Команда «деплой»

Когда пользователь пишет `деплой` — выполнить:

1. **Git push** — `/git-push` (add + commit + push)
2. **Deploy на сервер** — TODO: пока НЕ делать. Сервер ещё не настроен (nginx, SSL, systemd не сконфигурированы). Файлы конфигов подготовлены в `scripts/deploy/`. Когда пользователь скажет, что сервер готов — дополнить этот раздел командами деплоя (ssh + git pull + build + restart).
